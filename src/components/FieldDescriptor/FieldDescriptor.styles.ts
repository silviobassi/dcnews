import styled from "styled-components";

export const Wrapper = styled.div`
    color: #274060;

    span.Field {
        font-size: 12px;
        font-weight: 900;
    }

    p.Description {
        font-size: 14px;
    }
`